# lailla-crm

<!-- 
  comando utilizados:
  npx create-react-app meuprojeto (cria o projeto)
  npm install react-bootstrap bootstrap (instala o bootstrap)
  npm install react-router-dom (instala o router p/ navegacao; somente se o projeto não tiver o next!!!)

  p/ iniciar o next: npm run dev

  primereact:
  p/ instalar a biblioteca: npm install primereact
  p/ instalar os ícones da biblioteca: npm install primeicons

  biblioteca de ícones: npm i react-icons

  p/ instalar pg: npm install pg

  package.json:
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },

  PADRONIZAÇÃO DE NOMENCLATURA:
    nome de componentes: camelCase
    id & classes p/ css: train-case
    value e afins: snake_case
 -->