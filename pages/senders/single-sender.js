import SingleSender from '@/components/data/senders/SingleSender';

export default function SingleSenderPage({ singleSender, setSingleSender }) {
  return <SingleSender
    singleSender={singleSender} setSingleSender={setSingleSender}
  />;
}
